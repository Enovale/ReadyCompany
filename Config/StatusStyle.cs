namespace ReadyCompany.Config
{
    // Should be [Flags] but LethalConfig doesn't support it right now
    public enum StatusStyle
    {
        HudText,
        Popup,
        Both,
        None
    }
}